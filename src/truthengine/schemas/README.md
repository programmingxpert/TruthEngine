# Schemas

This folder contains Pydantic models for HTTP request and response shapes.

Current scope:

- Health response schema.

Schemas should stay transport-focused. Domain entities should live in future domain modules once those modules are intentionally introduced.

