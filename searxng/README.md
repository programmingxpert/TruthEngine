# SearXNG Local Search

This folder contains the minimal SearXNG configuration used by Docker Compose.

TruthEngine uses SearXNG as the local, replaceable `SearchProvider` implementation. The backend calls the SearXNG JSON search endpoint and does not require a paid search API.

